/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // below is a list of domains from where u could call images from
  images: [],
}

module.exports = nextConfig
